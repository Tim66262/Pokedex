package com.example.brippp.pokedex.Databse;

public class Database {
/*
    public long add(Badi badi) throws SQLException {
        db.beginTransaction();
        ContentValues values = new ContentValues();
        values.put(BadiSqlTable._ID, badi.getId());
        values.put(BadiSqlTable.COLUMN_NAME, badi.getName());
        values.put(BadiSqlTable.COLUMN_KANTON, badi.getKanton());
        values.put(BadiSqlTable.COLUMN_ORT, badi.getOrt()); long newId = db.insertOrThrow( BadiSqlTable.TABLE_NAME, null, values); return newId;
    }
*/
    }
